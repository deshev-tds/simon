# /etc/sudoers.d/simon-llama-ctl
#
# Allow the Simon backend (running as user "simon") to control llama.cpp running
# in a toolbox container owned by user "deshev", without prompting for a password.
#
# Adjust usernames and the /opt/simon path as needed.
#
# Install:
#   sudo install -m 0440 -o root -g root scripts/llama_ctl.sudoers.example /etc/sudoers.d/simon-llama-ctl
#
# Validate:
#   sudo visudo -cf /etc/sudoers.d/simon-llama-ctl
#
simon ALL=(deshev) NOPASSWD: /opt/simon/scripts/llama_ctl.sh *

